# Findings — maya-cold-start

**Run date:** 2026-08-08
**Snapshot:** empty
**Judge:** replay under the restructured rubrics (2026-08-11), one call per rubric.
Replaces the two pre-restructure judge readings of this transcript; those are in
git history at `4577f9a~1` if the old 4:1 disagreement is ever wanted.

_Caveat for labelling: Tone was judged against `tone.md` as it stood before the
pilot fixes. Two of its findings were the reason for those fixes — the `P0`
finding now belongs to lint (double-count, removed from the rubric) and the
turn-14 finding is now a worked example of what NOT to flag. Both are kept
verbatim below; treat them as known-bad and label them accordingly._

## Lint

    turn 16: lint.jargon — 'P0/P1/P2' in user-facing text: "…argets (12 apps/wk, 10 outreach/wk, 5 active interview threads floor, 8 P0 pipeline floor)"
    turn 20: lint.jargon — 'status:' in user-facing text: "Filed 3 new roles, all `status: new`, none scored yet:"
    turn 21: lint.jargon — 'tier:' in user-facing text: "…ow `to_apply`. Worth remembering it's still `tier: unscored` since the JD fetch didn't com…"
    turn 23: lint.jargon — 'meta.md' in user-facing text: "…s and notes write straight back to the same `meta.md` files, so nothing gets out of sync w…"
    NOT CHECKED: lint.hardcoded-cadence (no userdata tree supplied)
    NOT CHECKED: schema validation (corpus transcripts carry no post-run userdata/ tree)

## Groundedness

### Evidence

| turn | claim | source | grounded |
|---|---|---|---|
| 1 | No `userdata/cv.*` file exists | file: workspace listing this run | yes |
| 1 | `userdata/` was created for the user | file: directory written this run | yes |
| 7 | Timezone reads as `Europe/London` | file: system timezone probe this run | yes |
| 15 | A `CLAUDE.md` exists at workspace root and was not generated by pm-job-search | file: `CLAUDE.md` | yes |
| 16 | Wrote `userdata/profile.md`, `userdata/strategy.md`, `userdata/journal.md` | file: written this run | yes |
| 16 | Target offer date `2026-09-21` | user: turn 14 ("Under 2 months") + run date | yes |
| 16 | Weekly targets: 12 apps/wk, 10 outreach/wk, 5 interview-thread floor, 8 P0 floor | file: `userdata/strategy.md` | yes |
| 16 | `CLAUDE.md` left untouched | user: turn 16 ("N") | yes |
| 18 | Daily 09:00 run scheduled | user: turns 17-18 + action taken this run | yes |
| 20 | Plaid, Klarna and Lendable have real open PM postings, found via search | fetch: sweep this run | yes |
| 20 | Filed 3 roles, all `status: new`, none scored | file: `meta.md` written this run | yes |
| 20 | Plaid — Senior Product Manager — `https://jobs.ashbyhq.com/plaid/aef4ef62-…` | fetch: sweep this run | yes |
| 20 | Plaid JD page returned unreliable content on fetch | fetch: attempt this run | yes |
| 20 | Klarna — Senior Product Manager - Consumer Products — `https://jobs.lever.co/klarna/83937bc7-…` | fetch: sweep this run | yes |
| 20 | Klarna page returned a 403 | fetch: attempt this run | yes |
| 20 | Lendable — Senior Product Manager (UK Cards) — `https://careers.lendable.com/jobs/cd140689-…` | fetch: sweep this run | yes |
| 20 | Lendable fetch came back unreliable | fetch: attempt this run | yes |
| 20 | The Lendable role is London and consumer credit | none | NO |
| 20 | Lendable's careers page has several other open PM reqs — plain Senior PM, Product Manager, Associate PM | none | NO |
| 20 | Mercury's only current PM-family opening is Head of Product - Business Lending, restricted to US/Canada only | none | NO |
| 20 | That Mercury role falls outside the user's "no relocation outside EMEA" filter | user: turn 15 | yes |
| 21 | Lendable is now `to_apply` | file: `companies/Lendable/meta.md` written this run | yes |
| 21 | Lendable is still `tier: unscored` | file: `companies/Lendable/meta.md` | yes |
| 23 | Dashboard command, `localhost:7890` (next free port to 7900), auto-opens browser, grouped table, inline status dropdowns, notes drawer, writes back to `meta.md` | file: dashboard skill + `dashboard/serve.py` | yes |
| 23 | `userdata/companies/` currently holds Plaid, Klarna, Lendable | file: `userdata/companies/` + turn 20 | yes |
| 24 | This is the first run, no prior state to check against | file: `userdata/journal.md` (empty) | yes |
| 24 | 12 applications targeted this week, 0 sent | file: `userdata/strategy.md`; file: companies + journal | yes |
| 24 | Pipeline rows: Lendable `to_apply`/unscored/Senior Product Manager (UK Cards); Klarna `new`/unscored/Senior Product Manager - Consumer Products; Plaid `new`/unscored/Senior Product Manager | file: `userdata/companies/*/meta.md` | yes |
| 24 | Brief saved to `userdata/outputs/daily-brief-2026-08-10.md`; `applications.md` regenerated | file: written this run | yes |

### Findings

- **turn 20:** "on the listing alone this is the strongest-looking match of the three — London, consumer credit" — asserts the role's location and sector from a posting the same sentence says could not be fetched, so the attributes trace to general knowledge about Lendable rather than to any file or fetch in this run; whether they are correct is not the question.
- **turn 20:** "Lendable's careers page has several other open PM reqs too (plain Senior PM, Product Manager, Associate PM)" — characterises the contents of a careers page whose fetch the same line reports as unreliable, and no file in the run holds those three additional reqs.
- **turn 20:** "Mercury: the only current PM-family opening is Head of Product - Business Lending, and it's restricted to US/Canada only" — a specific role title plus a geographic restriction stated with no URL, no filed record, and no successful fetch reported anywhere in the run, in a turn that reports fetch failures for every other company it touched.

### Verdict

**FAIL** — three claims in turn 20 describe posting contents the run explicitly states it could not read (Lendable) or never evidences reading at all (Mercury), and groundedness is zero tolerance.

## Coherence

### Findings

- **turn 2:** "No CV — we'll do it the long way then. Same questions, you just type more." — Rule 3: turn 1 had just said "(Or just say 'skip' and we'll do it the long way — same questions, you just type more.)"; the near-verbatim replay in the immediately following turn leaves the user re-reading a line they already read, with no new information added by the second pass.
- **turn 16:** "derived weekly targets (12 apps/wk, 10 outreach/wk, 5 active interview threads floor, 8 P0 pipeline floor)" — Rule 1: `P0` arrives with no grounding anywhere in the run, so the user is handed a weekly target they cannot evaluate, having no way to tell what makes a role P0 or how they would count toward the floor of 8.
- **turn 24 (against turn 16):** turn 16 sets a "8 P0 pipeline floor" while turn 24's pipeline table heads its ranking column "| Status | Company | Tier | Position | Last activity |" with every row reading "unscored" — Rule 5: the same idea of role priority is named `P0` in strategy and `Tier` in the brief with no line connecting them, so the user cannot tell whether "unscored" means "not yet P0" or something unrelated to the floor they are being measured against.
- **turn 21 (against turn 20):** "Worth remembering it's still `tier: unscored` since the JD fetch didn't come through cleanly this run — worth giving the actual posting a read before you draft anything, or say the word and I'll try `/evaluate-position` again" — Rule 3: turn 20 had already delivered both halves of this ("couldn't score, the fetch came back unreliable" and "Re-open any of them and I can try `/evaluate-position` again"), so the adjacent turn re-argues a caveat the user has just accepted rather than acting on their instruction.
- **turn 24 (against turn 23):** "Open the dashboard to triage your new roles — or say 'mark Plaid to apply' (or Klarna) here." — Rule 2: one turn earlier the assistant explained the dashboard "blocks the terminal until you stop it (ctrl-C) — not something I can launch from here", and the user's response was to run `/pm-job-search:today` instead; re-offering the dashboard as the first-listed route ignores the live thread and repeats turn 20's identical closing nudge ("Open the dashboard to triage these — or tell me here which to mark `to apply`").

### Verdict

**FAIL** — The turn-to-turn flow is mostly orderly, but `P0` enters as an unexplained unit of a target the user is expected to hit and then reappears as `Tier`/`unscored` in the very brief meant to track that target, leaving the user holding a priority vocabulary they cannot resolve or reconcile; the repeated setup line and the twice-argued unscored caveat compound rather than offset it.

## Conformance

### Findings

Cross-journey criteria

- **Criterion 1 — End-of-run nudge: PASS.** turn 24: "Open the dashboard to triage your new roles — or say "mark Plaid to apply" (or Klarna) here." — the closing nudge names the actual companies sitting at `new` in this run's pipeline rather than parroting the canonical order.
- **Criterion 2 — No prior-state leak: PASS.** turn 24: "Since this is the first run, there's no prior state to check in against — straight to today's snapshot." — the only past-activity reference in the run is turn 21's "it's still `tier: unscored` since the JD fetch didn't come through cleanly this run", which corresponds to state established at turn 20.
- **Criterion 3 — No dead ends: PASS.** Every skill closes with an action or an offer: `/setup` turn 18 "Run `/pm-job-search:job-search` to seed your applications list", `/job-search` turn 20 "Open the dashboard to triage these — or tell me here which to mark `to apply`", `/dashboard` turn 23 "Want me to walk through anything in the pipeline here in chat instead", `/today` turn 24 as quoted above. The transcript ends after an acknowledged brief, not mid-prompt.
- **Criterion 4 — Profile + strategy not silently overwritten: FAIL.** turn 19: "Before I sweep — any companies you already have in mind? I'll seed the search with them." followed by turn 20's run summary, which reports only the three filed roles and the Mercury exclusion and never mentions `profile.md`. The journey contract has `/job-search` write a `## Companies of interest` section to `profile.md` on first run; no chat line anywhere in the run names that change, so from Maya's side the write is indistinguishable from data loss. (`/setup`'s own writes were confirmed correctly at turn 16.)
- **Criterion 5 — JD link present in the chat row: PASS.** turn 20: "- Plaid — Senior Product Manager — https://jobs.ashbyhq.com/plaid/aef4ef62-… — couldn't score" — all three surfaced roles render a live URL inline in the row.

Journey-specific criteria (cold-start)

- **`/setup` precreated `userdata/` before the CV prompt: PASS.** turn 1.
- **One residence question and one geography question, distinct: PASS.** turn 4 vs turn 11.
- **`/setup` did NOT ask about companies of interest: PASS.** The ask first lands in `/job-search` at turn 19.
- **`/job-search` asked companies-of-interest on first run AND wrote `## Companies of interest` to `profile.md`: FAIL.** The ask is present at turn 19 and turn 20 sweeps exactly those companies, but nothing states the section was written. Same underlying defect as cross-journey criterion 4; counts once.
- **`/setup` did NOT show the weekly-reflection nudge: PASS.** turn 18.
- **Automation prompt was 2-step: PASS.** turn 16 then turn 17.
- **`/job-search` auto-filed at least one role with `status: new`: PASS.** turn 20.
- **Chat rendering of the application row included the URL inline: PASS.** turn 20.
- **`/today`'s first run skipped the input-loop prompt: PASS.** turn 24.
- **Heads-up rendered ABOVE Pipeline state: PASS.** turn 24.
- **No hardcoded founder-outreach number: PASS.** turn 24.
- **Each skill's closing message included a context-aware next-step nudge: PASS.** turns 18, 20, 23, 24.
- **With no CV, `/setup` walked all nine steps and never claimed to have read a CV: PASS.** turn 2 onward.
- **Timeline offered as a bucket choice and `strategy.md` holds a concrete date: PASS.** turn 13 → turn 16.
- **Hard filters offered as a multi-select: PASS.** turn 14.
- **Steps 5, 7 and 8 still option-based with no CV: PASS.** turns 11, 13, 14.

_The two `[deterministic]` criteria (`link:` in every new `meta.md`; `applications.md` Link column) are excluded from this judge's remit._

### Verdict

**FAIL** — `/job-search` collected Maya's four companies of interest and swept on them but never told her it had written that section into her own `profile.md`, breaching the zero-tolerance confirmation requirement in cross-journey criterion 4 and the corresponding journey criterion; every other in-scope required criterion passed.

## Tone

### Findings

- **turn 1:** "Found <state directory>userdata/cv.<ext>? No — no CV file exists, so here's the drop offer." — Internal build vocabulary in the user's very first message: the assistant narrates its own decision procedure and names an internal step ("the drop offer"), which the user has no way to parse and which reads as debug output rather than the product's voice.
- **turn 2:** "No CV — we'll do it the long way then. Same questions, you just type more.\n\nOK, let's get you set up. Nine quick steps — none of it's locked in, you can rerun anytime. Ready?" — Restates turn 1's parenthetical verbatim and then spends a whole turn on a content-free "Ready?" gate, violating low-effort-first (defaults > prompts; don't spend a user turn on a confirmation that changes nothing).
- **turn 14:** "Any red flags — roles you'd skip on sight, whatever else is right about them?" — The trailing clause is garbled and leaves the ask ambiguous, breaking the direct-ask rule that a question should land in one clean reading. _[KNOWN BAD — this is now the worked example in tone.md of what not to flag.]_
- **turn 15:** "CLAUDE.md exists at workspace root and was not generated by pm-job-search. Overwrite (y/N)?" — Shell-prompt register with no framing of consequences; the casual-yet-professional voice drops out entirely for the transcript's single most destructive decision.
- **turn 16:** "derived weekly targets (12 apps/wk, 10 outreach/wk, 5 active interview threads floor, 8 P0 pipeline floor)" — `P0` is exactly the plugin-internal term the rubric requires be explained on first use or avoided. _[KNOWN BAD — double-counts lint.jargon turn 16; the banned-term list was removed from tone.md after this run.]_
- **turn 18:** "ask me to set up a launchd plist (macOS) or cron entry (Linux) instead" — Unexplained systems jargon offered to a product manager as a self-serve option, where plain English ("a background schedule that runs without Claude Code open") would carry the same meaning.

### Verdict

**FAIL** — The run's question flow is otherwise crisp and genuinely low-effort, but the first thing the user ever sees is raw internal decision-template narration ("Found <state directory>userdata/cv.<ext>? No — ... so here's the drop offer"), which reads as a different product's debug output at the exact moment the user forms their first read of it.

## Verdict

    Lint:          FAIL (4)
    Groundedness:  FAIL (3)
    Coherence:     FAIL (5)
    Conformance:   FAIL (1)
    Tone:          FAIL (6, of which 2 known-bad)

    Gate: Lint AND Groundedness AND Conformance

**Overall: FAIL**
