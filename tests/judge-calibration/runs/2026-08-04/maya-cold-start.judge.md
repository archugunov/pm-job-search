# Findings — maya-cold-start

**Run date:** 2026-08-04
**Snapshot:** empty
**Judge:** replay under the restructured rubrics (2026-08-12), one call per rubric.
Replaces the pre-restructure reading; that is in git history.

## Lint

    No lint findings.
    NOT CHECKED: lint.hardcoded-cadence (no userdata tree supplied)

## Groundedness

### Evidence

| turn | claim | source | grounded |
|---|---|---|---|
| 1 | `userdata/` was created | file: `userdata/` (created this run) | yes |
| 1 | no CV file is present in `userdata/` | file: `userdata/` (checked this run) | yes |
| 2 | setup is a nine-step flow | file: setup `SKILL.md` (read this run) | yes |
| 8 | timezone is Europe/London | user: turn 8 | yes |
| 11 | positioning: "a specialist in consumer credit and pricing mechanics" | user: turn 11 | yes |
| 12 | proof-point numbers: +18% MRR Q3; 12% decline rate on thin-file | user: turn 12 | yes |
| 12 | proof point #1 employer is "Current fintech (Series B)" | user: turn 12 | yes |
| 12 | proof point #2 employer is "Current fintech (Series B)" | none | NO |
| 13 | moat: consumer credit + thin-file underwriting, pricing/risk trade-offs first-hand | user: turn 13 | yes |
| 18 | `CLAUDE.md` exists at workspace root and was not generated by pm-job-search | file: `CLAUDE.md` (checked this run) | yes |
| 19 | `userdata/profile.md` holds identity, target role, salary, hard filters | file: `userdata/profile.md` (written this run) | yes |
| 19 | target offer date is `2026-09-15`, from the "Under 2 months" answer | file: `userdata/strategy.md` (written this run); user: turn 17 | yes |
| 19 | derived weekly targets: 12 apps/wk, 10 outreach/wk, 5 active interview threads floor | file: `userdata/strategy.md` (written this run) | yes |
| 19 | `userdata/journal.md` is empty | file: `userdata/journal.md` (written this run) | yes |
| 19 | `CLAUDE.md` left as-is, still the user's existing file | user: turn 19 | yes |
| 23 | `## Companies of interest` added to profile with Plaid, Klarna, Lendable, Mercury | user: turn 23; file: `userdata/profile.md` (written this run) | yes |
| 24 | discovery returned 51 candidates across 10 site-scoped queries | fetch: discovery searches run this turn | yes |
| 24 | 25 candidates were fetchable and scorable; the rest were dropped | fetch: discovery searches run this turn | yes |
| 24 | filed 25 roles across 21 companies | file: the 25 `meta.md` written this run | yes |
| 24 | `applications.md` was regenerated | file: `userdata/outputs/applications.md` (written this run) | yes |

### Findings

- **turn 12:** "Current fintech (Series B)" — the second proof point was supplied by the user with no employer attached, so anchoring it to the same Series B fintech invents an employer that traces to no file, fetch, or user turn; the attribution may well be true, but correctness is not the question here, groundedness is.

### Verdict

**FAIL** — one claim in the table is ungrounded (turn 12 attributes the user's unattributed second proof point to a specific employer), and this rubric is zero-tolerance.

## Coherence

### Findings

- **turn 20:** "Want `/pm-job-search:today` to run automatically every day? (y / n)" — Rule 1: `/today` has never appeared in the conversation (turn 19's file list doesn't mention it), so the user is asked to commit to a daily automated run of something they cannot evaluate; the explanation arrives only one turn later, at turn 21 — "or `/pm-job-search:today` right now if you'd rather see a daily brief first" — after the decision was already made.
- **turn 19:** "or ask `pm-job-search:career-coach` to help you set anti-goals and checkpoints." — Rule 1: three things land cold in one clause (the `career-coach` agent, "anti-goals", "checkpoints"), none introduced earlier and none explained here, leaving the user with a suggested next action whose value they have no way to judge.

### Verdict

**FAIL** — the setup body is clean and well-sequenced, but the closing two turns hand the user four unresolvable terms and, at turn 20, make them answer y/n on a skill they have never seen, with the explanation arriving only after the answer.

## Conformance

### Findings

- **turn 24:** "Filed 25 roles across 21 companies, regenerated `applications.md`." — criterion 1 (end-of-run nudge) fails: the last skill to run in the journey closed with a bare run summary and no state-aware next-step suggestion, which also breaks the journey criterion "each skill's closing message included a context-aware next-step nudge" (`/setup` did nudge at turn 21; `/job-search` did not).
- **"Loop ended: journey complete" section:** "`/dashboard` and `/today` were not re-run in this journey" — the [required] journey criterion "`/today`'s first run skipped the input-loop prompt entirely (no 'anything that moved since last time')" was never exercised even though the journey's own spine (`/setup` → `/job-search` → `/dashboard` → `/today`) put it in scope, which the rubric scores as FAIL rather than NOT EXERCISED.
- **"Loop ended: journey complete" section:** "`/today` against a freshly-written profile was already verified independently as release-handoff check §4.7" — the [required] journey criterion "`/today`'s brief rendered Heads-up section ABOVE Pipeline state" went unexercised in this run; a prior independent check is not evidence inside this transcript.
- **"Loop ended: journey complete" section:** "`/setup` → `/job-search` covered." — the [required] journey criterion "`/today` did NOT include a hardcoded founder-outreach number (no '10 founders')" was in scope for this journey and never exercised, so it fails.

### Verdict

**FAIL** — everything `/setup` and `/job-search` actually exercised conformed (userdata precreated before the CV prompt, distinct residence vs geography asks, no companies question in `/setup`, `## Companies of interest` asked and written by `/job-search`, no weekly-reflection nudge in `/setup`, 2-step automation prompt, roles auto-filed at `status: new`, URL inline in the chat rows, all nine steps walked with no CV, bucketed timeline resolving to `2026-09-15`, multi-select hard filters, and both profile and strategy writes confirmed in chat), but the run stopped after `/job-search` without the end-of-run nudge and left three [required] `/today` criteria unexercised while they were in scope.

## Tone

### Findings

- **turn 24:** "Discovery returned 51 real candidates across 10 site-scoped queries. Scoring ran in three batches. 25 candidates were fetchable and scorable; the rest were dropped." — Internal build vocabulary in user-facing copy: "site-scoped queries", "three batches", and "fetchable and scorable" narrate the plugin's own machinery, which the user has no reason to know about and cannot act on.
- **turn 23:** "Ready to kick off the sweep — I'll recheck any tracked companies and run discovery seeded with those four plus your target titles/industries. Say go whenever." — Violates defaults-over-prompts: the user already invoked the sweep and answered the seeding question, so a further "say go" gate buys a round-trip for a decision that was already made.
- **turn 2:** "OK, let's get you set up. Nine quick steps — none of it's locked in, you can rerun anytime. Ready?" — Same pattern at the front of the flow: "Ready?" is a confirmation gate rather than an ask that collects anything, and it delays the first real question by a full turn.

### Verdict

**PASS** — The voice is consistently terse, direct and contraction-friendly, with clean single asks and good auto-detect-then-confirm behaviour (turn 7); the three findings are two redundant confirmation gates and one lapse into internal machinery vocabulary, none of which a user would read as the product suddenly speaking in a different register.

## Verdict

    Lint:          PASS
    Groundedness:  FAIL (1)
    Coherence:     FAIL (2)
    Conformance:   FAIL (1)
    Tone:          PASS

    Gate: Lint AND Groundedness AND Conformance

**Overall: FAIL**
